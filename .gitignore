# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# certs
*.pem

# env files — real secrets never get committed; keep a checked-in .env.example instead
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# claude code
# local-only overrides, not shared with the repo
.claude/settings.local.json
# local-only guardrail, imported by CLAUDE.md, never committed
CLAUDE.local.md
